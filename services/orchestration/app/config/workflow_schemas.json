{
  "run_zoom_av_workflow": {
    "meeting_id": {
      "type": "string",
      "default": "",
      "description": "Zoom meeting ID (digits only, spaces optional)"
    },
    "passcode": {
      "type": "string",
      "default": "",
      "description": "Meeting passcode / password if required"
    },
    "display_name": {
      "type": "string",
      "default": "Participant",
      "description": "Name to display on the Zoom join screen"
    },
    "wait_seconds": {
      "type": "integer",
      "default": 60,
      "description": "Seconds to remain in the meeting or lobby before the workflow stops"
    }
  },
  "run_zoom_workflow": {
    "meeting_id": {
      "type": "string",
      "default": "",
      "description": "Zoom meeting ID (digits only, spaces optional)"
    },
    "passcode": {
      "type": "string",
      "default": "",
      "description": "Meeting passcode / password if required"
    },
    "display_name": {
      "type": "string",
      "default": "Participant",
      "description": "Name to display on the Zoom join screen"
    },
    "wait_seconds": {
      "type": "integer",
      "default": 60,
      "description": "Seconds to remain in the meeting or lobby before the workflow stops"
    }
  },
  "test_youtube_workflow": {
    "url": {
      "type": "string",
      "default": "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
      "description": "YouTube video URL to open and play"
    },
    "watch_seconds": {
      "type": "integer",
      "default": 30,
      "description": "Seconds to keep playback running before stopping"
    }
  },
  "test_youtube_general_workflow": {
    "url": {
      "type": "string",
      "default": "https://www.youtube.com/watch?v=nXbHOUeFe8s",
      "description": "YouTube video URL to watch in the general workflow"
    },
    "watch_time": {
      "type": "integer",
      "default": 30,
      "description": "Duration in seconds to watch the YouTube video"
    }
  },
  "test_ndt_workflow": {
    "download": {
      "type": "boolean",
      "default": true,
      "description": "Run the download direction of the NDT test (true/false)"
    },
    "upload": {
      "type": "boolean",
      "default": true,
      "description": "Run the upload direction of the NDT test (true/false)"
    }
  },
  "test_shell_workflow": {
    "host": {
      "type": "string",
      "default": "8.8.8.8",
      "description": "Ping target host or IP address"
    },
    "count": {
      "type": "integer",
      "default": 20,
      "description": "Number of ping packets to send"
    }
  },
  "test_wget_workflow": {
    "url": {
      "type": "string",
      "default": "https://speed.cloudflare.com/__down?bytes=10485760",
      "description": "URL to download via wget (use a stable test endpoint for repeatable runs)"
    },
    "output_file": {
      "type": "string",
      "default": "/dev/null",
      "description": "Destination path for the response body; /dev/null discards it"
    },
    "timeout_seconds": {
      "type": "integer",
      "default": 30,
      "description": "Per-connection timeout in seconds"
    },
    "tries": {
      "type": "integer",
      "default": 1,
      "description": "Maximum number of retry attempts"
    }
  }
}
