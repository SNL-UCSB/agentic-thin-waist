#!/usr/bin/env bash
# =============================================================================
# config.env — netReplica Distributed Setup Configuration
# THIS FILE IS MANAGED BY ANSIBLE. Do not edit manually.
# Source: roles/common/templates/config.env.j2
# =============================================================================

# -----------------------------------------------------------------------------
# UPSTREAM SERVER  (Edge Router / NAT gateway)
# -----------------------------------------------------------------------------
UPSTREAM_SWITCH_IFACE="{{ upstream_switch_iface }}"
UPSTREAM_INTERNET_IFACE="{{ upstream_internet_iface }}"
UPSTREAM_SWITCH_IP="{{ upstream_switch_ip }}"
SWITCH_SUBNET="{{ switch_subnet }}"
NS_NAME="{{ ns_name }}"
VETH_INTERNET_HOST="{{ veth_internet_host }}"
VETH_INTERNET_NS="{{ veth_internet_ns }}"
VETH_SWITCH_HOST="{{ veth_switch_host }}"
VETH_SWITCH_NS="{{ veth_switch_ns }}"
VETH_INTERNET_HOST_IP="{{ veth_internet_host_ip }}"
VETH_INTERNET_NS_IP="{{ veth_internet_ns_ip }}"
VETH_SWITCH_HOST_IP="{{ veth_switch_host_ip }}"
VETH_SWITCH_NS_IP="{{ veth_switch_ns_ip }}"
POLICY_TABLE_NAME="{{ policy_table_name }}"
POLICY_TABLE_ID="{{ policy_table_id }}"
GRE_CLIENT_SUBNET="{{ gre_client_subnet }}"
DOWNSTREAM_SWITCH_IP="{{ downstream_switch_ip }}"

# -----------------------------------------------------------------------------
# DOWNSTREAM SERVER  (Core Router / Aggregator)
# -----------------------------------------------------------------------------
DOWNSTREAM_SWITCH_IFACE="{{ downstream_switch_iface }}"
DOWNSTREAM_INTERNET_IFACE="{{ downstream_internet_iface }}"
DOWNSTREAM_SWITCH_IP="{{ downstream_switch_ip }}"
SERVER_INTERNET_IP="{{ server_internet_ip }}"
PINOT_IPS="{{ pinot_ips }}"
GRE_TUNNEL_BASE="{{ gre_tunnel_base }}"
GRE_TUNNEL_PREFIX="{{ gre_tunnel_prefix }}"
DOCKER_NETWORK_NAME="{{ docker_network_name }}"
DOCKER_SUBNET="{{ docker_subnet }}"
DOCKER_MTU="{{ docker_mtu }}"

# -----------------------------------------------------------------------------
# LIBREQOS SERVER  (Traffic Shaper)
# -----------------------------------------------------------------------------
LIBREQOS_SWITCH_IFACE="{{ libreqos_switch_iface }}"
LIBREQOS_MGMT_IFACE="{{ libreqos_mgmt_iface }}"
LIBREQOS_VLAN_UPSTREAM_IP="{{ libreqos_vlan_upstream_ip }}"
LIBREQOS_VLAN_DOWNSTREAM_IP="{{ libreqos_vlan_downstream_ip }}"
LIBREQOS_ISP_CONFIG="{{ libreqos_isp_config }}"
LIBREQOS_LQOS_CONF="{{ libreqos_lqos_conf }}"

# -----------------------------------------------------------------------------
# SWITCH  (Managed L2 switch)
# -----------------------------------------------------------------------------
SWITCH_SERIAL_DEVICE="{{ switch_serial_device }}"
SWITCH_UPSTREAM_PORT="{{ switch_upstream_port }}"
SWITCH_DOWNSTREAM_PORT="{{ switch_downstream_port }}"
SWITCH_LIBREQOS_PORT="{{ switch_libreqos_port }}"
VLAN_UPSTREAM_ID="{{ vlan_upstream_id }}"
VLAN_DOWNSTREAM_ID="{{ vlan_downstream_id }}"
