{
  "specification": "test shell workflow",
  "states": [
    {
      "checks": [
        {
          "type": "always_true",
          "params": {}
        }
      ],
      "actions": [
        {
          "type": "ping",
          "params": {
            "host": "{{host}}",
            "count": "{{count}}"
          }
        }
      ]
    }
  ],
  "parameters": ["host", "count"]
}
