name: CTP Service - Build & Test

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - 'services/ctp-service/**'
      - 'shared/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create repo .env from example
        run: cp .env.example .env

      - name: Build Docker image
        run: docker build -f services/ctp-service/Dockerfile -t ctp-service .

      - name: Run tests
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/services/ctp-service/tests:/app/tests \
            ctp-service \
            python -m pytest tests/ -v --tb=short

      - name: Start service via Docker Compose
        run: docker compose up -d --wait ctp-service
