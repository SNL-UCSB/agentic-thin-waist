# Tasks NNN — <name>
> Status: Draft | Created: <date> | Source of truth: THIS FILE (GitHub Issues
> are generated from it via /speckit-taskstoissues; one-way, never hand-edit
> issues; reconcile closures at the milestone gate)
IDs use this repo's card namespace C-<n> (not T###). TEST TASKS ARE MANDATORY:
every implementation card is preceded by its supervisor-authored failing
suite in tests/cards/test_<id>.py (constitution Art. VII).
- [ ] C-<n> <what> (S|M|L, student|supervisor[:name]) — suite: tests/cards/test_c<n>.py
