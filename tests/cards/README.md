# Card test suites
One file per WBS card: `test_<card-id>.py` (e.g. `test_c101.py`), authored by
the supervising grad student BEFORE the card is assignable (WBS rule 1).
A card is done when its suite + the shared/models contract tests pass in CI.
